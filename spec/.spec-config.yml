canonical_language: de
languages: [de, en]
spec_root: spec

# Portfolio-inherited spec layer (nolte/claude-shared#339, mechanism: claude-shared#340).
# This repo references portfolio-wide specs from the nolte-shared hub instead of
# copying them verbatim. The previously copied `project/branching-model` and
# `project/parallel-working-copies` specs were deleted; they now resolve from the
# inherited set. No `overrides:` are declared — the former local copies differed from
# the hub canonical only in copy artefacts (ghost-reference neutralisation and
# `claude-shared`->`claude-home-assistant` example localisation), both of which the
# inherited model resolves on its own.
#
# GATING — this declaration only resolves cleanly once the hub release pinned below:
#   1. carries `Portfolio-Scope: portfolio` on `project/branching-model` and
#      `project/parallel-working-copies`, AND on every sibling spec they cross-reference
#      (else those refs become ghost references = Critical): project/pull-request-workflow,
#      project/release-automation, project/workflow-health, project/project-structure,
#      project/quality-gate, project/sprint, project/release-artifact, project/spec-readiness,
#      project/docs-freshness, claude/review-plan, claude/resumable-work,
#      claude/research-triangulate, claude/permission-allowlist;
#   2. ships the `Portfolio-Scope: portfolio` spec subset with the plugin payload.
# v0.1.9 is the planned next claude-shared release (open draft) that carries the #340
# mechanism; bump `ref` to the exact release that first ships the scoped cluster.
# PR #14 stays Draft until that release is live (floating/stale ref is a drift Warning).
inherits:
  - source: nolte-shared
    ref: v0.1.9
