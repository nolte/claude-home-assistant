# See https://pre-commit.com for usage and config reference.
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents, --unsafe]
      - id: check-json
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: [--fix=lf]

  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [-d, relaxed]

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.42.0
    hooks:
      - id: markdownlint
        args:
          - --disable
          - MD013  # line-length
          - MD022  # blanks-around-headings
          - MD032  # blanks-around-lists
          - MD033  # no-inline-html
          - MD036  # no-emphasis-as-heading
          - MD040  # fenced-code-language
          - MD041  # first-line-h1
          - MD049  # emphasis-style (allow underscore)
          - --

  - repo: local
    hooks:
      - id: guard-primary-checkout
        name: primary checkout must stay on develop
        entry: scripts/guard_primary_checkout.sh
        language: system
        always_run: true
        pass_filenames: false
      # Byte-compare the vendored catalog generator against the pinned canonical
      # copy in the nolte-shared hub. Network-tolerant locally (skips offline);
      # CI enforces it with --require-network. Runs only when the generator changes.
      - id: catalog-generator-drift
        name: catalog generator must match the pinned canonical hub copy
        entry: python3 scripts/docs/check_catalog_generator_drift.py
        language: system
        files: ^scripts/docs/gen_catalog\.py$
        pass_filenames: false
